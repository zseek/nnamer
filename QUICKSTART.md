# Nnamer 快速开始

## 当前状态

✅ 应用已成功启动！开发服务器正在运行。

## 已实现的功能

### 后端 (Rust)
- ✅ 文件扫描：扫描指定目录的 TXT 文件
- ✅ 名称标准化：清理非法字符、去除扩展名
- ✅ LLM 客户端：调用 OpenAI 兼容 API
- ✅ 回收站操作：安全移除文件
- ✅ 两阶段重命名：防止冲突和覆盖
- ✅ 设置管理：保存/读取配置

### 前端 (React)
- ✅ 应用框架：React + TypeScript + Zustand
- ✅ 目录选择组件
- ✅ 分析控制组件
- ✅ 冲突处理组件
- ✅ 执行预览组件
- ✅ 设置界面组件

## 下一步需要完成

### 1. 文件表格组件
当前缺少核心的文件列表表格，需要实现：
- 显示所有扫描到的文件
- 显示原文件名、建议名称、大小、状态
- 支持排序、筛选、多选
- 支持内联编辑建议名称

### 2. 状态派生逻辑
需要在前端实现文件状态的自动计算：
- 冲突检测（同一建议名称）
- 名称相同检测（建议名称与原名称相同）
- 名称无效检测（包含非法字符）

### 3. API 集成测试
- 测试设置保存和加载
- 测试目录扫描
- 测试 LLM 分析（需要实际 API Key）
- 测试文件重命名

## 如何测试

### 1. 配置 API
点击"设置"按钮，配置：
- Base URL（例如：`https://api.openai.com/v1`）
- API Key
- 模型名称（例如：`gpt-3.5-turbo`）

### 2. 选择测试目录
创建一个测试目录，放入一些示例 TXT 文件：
```
测试目录/
├── 诡秘之主完整版.txt
├── 诡秘之主_章节1-100.txt
├── 斗破苍穹[全本].txt
├── 斗破苍穹_网站A_更新至500章.txt
└── 未知小说123.txt
```

### 3. 执行分析
1. 选择上述测试目录
2. 查看扫描到的文件列表
3. 点击"分析全部"调用 LLM
4. 查看建议名称和冲突状态
5. 使用"保留最大文件"处理冲突
6. 预览并执行重命名

## 开发命令

```bash
# 启动开发服务器（已在运行）
pnpm tauri dev

# 停止开发服务器
按 Ctrl+C

# 重新构建前端
pnpm build:frontend

# 运行测试
cargo test --manifest-path src-tauri/Cargo.toml --lib
```

## 故障排查

### 应用无法启动
- 检查是否有其他进程占用 1420 端口
- 尝试删除 `node_modules` 和 `target` 目录后重新安装

### API 调用失败
- 检查 API Key 是否正确
- 检查 Base URL 是否正确（带 `/v1`）
- 检查网络连接

### 文件操作失败
- 确保文件未被其他程序占用
- 检查目录权限
- Windows 可能需要管理员权限

## 技术栈

- **Tauri 2**: 桌面应用框架
- **React 19**: UI 框架
- **TypeScript 5.8**: 类型安全
- **Zustand**: 状态管理
- **Tailwind CSS 4**: 样式系统
- **Rust**: 后端逻辑
- **reqwest**: HTTP 客户端
- **trash**: 回收站支持

## 项目结构

```
Nnamer/
├── src/                    # React 前端
│   ├── features/          # 功能模块
│   ├── shared/            # 共享代码
│   ├── store.ts           # Zustand store
│   └── App.tsx            # 主应用
├── src-tauri/             # Rust 后端
│   └── src/
│       ├── files.rs       # 文件操作
│       ├── llm.rs         # LLM 客户端
│       ├── naming.rs      # 名称处理
│       ├── settings.rs    # 设置管理
│       └── lib.rs         # 主入口
└── dist/                  # 构建输出
```

## 许可证

待定
